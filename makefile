##
# made by @paulo-e, @pauloe
# the configuration is pretty simple
# and can be changed bellow
#
# @oxygen
# @version git

C = rustc				# compiler being used
DESTDIR = build			# destination folder for built applications

all:
	mkdir -pv $(DESTDIR)

# end
